{
  "name": "omnilert-website",
  "private": true,
  "scripts": {
    "up:dev": "pnpm -C apps/api migrate:master && pnpm -C apps/api migrate:tenant && turbo dev",
    "up:prod": "pnpm -C apps/api migrate:master && pnpm -C apps/api migrate:tenant && turbo build && concurrently \"pnpm -C apps/api start\" \"pnpm -C apps/web preview -- --host\"",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.15.0"
}
